# Production reverse proxy — TLS termination for ActiveDecoy
# Set CONSOLE_DOMAIN to your FQDN (e.g. activedecoy.lab.local)

{$CONSOLE_DOMAIN:localhost} {
	encode gzip
	header {
		-Server
	}
	reverse_proxy app:8000
}
